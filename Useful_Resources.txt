System Connected
    module -> 'sys'
        1. sys.version - for getting current version of Python
        2. sys.version_info - for getting additional details about Python version


Date Connected
    module -> 'datetime'
        1. date.today() - returns current date
        2. datetime.now() - returns current date/time/timezone
        3. strftime() - formats the datetime object as a string with the desired format


Math Connected
    module -> 'math'
        1. math.pi - returns value of 'pi' number
        2. math.pow() - gives power of number


String Connected
    1. split() - splits the string at the given argument

